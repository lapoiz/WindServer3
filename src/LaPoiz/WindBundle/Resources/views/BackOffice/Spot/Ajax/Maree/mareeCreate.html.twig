    <h3>URL pour récupérer les prévisions de marées</h3>*

    JAMAIS UTILISE ?????????????????????????????

    <div class="row">
        <div class="col-sm-12">
            {{ form_start(form, {
                'style': 'vertical',
                'label_col': 2,
                'widget_col': 8,
                'methode' : 'POST',
                'action' : path('_bo_ajax_maree_create', {'id':spot.id})
            })}}

            {{ form_errors(form, { 'global_errors': true }) }}

            {{ form_end(form) }}

        </div>
    </div>

    <script>
        $("#maree_form").on('submit', function(e){

            $('#result').html('<img src="{{ asset('bundles/lapoizwind/images/loading.gif') }}" alt="Loading Ajax data" />');

            e.preventDefault();
            var $this = $(this);

            $.ajax({
                url: $this.attr('action'),
                type: $this.attr('method'),
                data: $this.serialize(),
                success: function(html) {
                    $('#result').html(html);
                    if (create == 1) {
                        location.reload();
                    }
                }
            });
        })
    </script>
